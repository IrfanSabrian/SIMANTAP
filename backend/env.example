# Database Configuration
# Railway Database (Primary)
DATABASE_URL="postgresql://postgres:post123@localhost:5432/kubu_raya?schema=public"
# Fallback to localhost if Railway fails
FALLBACK_DATABASE_URL="postgresql://postgres:post123@localhost:5432/kubu_raya?schema=public"

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# Rate Limiting
# Set RATE_LIMIT_ENABLED=false untuk menonaktifkan rate limiting
RATE_LIMIT_ENABLED=true
# Window waktu dalam milliseconds (default: 900000 = 15 menit)
RATE_LIMIT_WINDOW_MS=900000
# Maksimal request per window (default: 1000)
RATE_LIMIT_MAX_REQUESTS=1000

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=irfansabrian9@gmail.com
SMTP_PASS=kort gqgv wicw vjmc
SMTP_FROM_NAME=SIMANTAP KUBU RAYA
SMTP_FROM_EMAIL=irfansabrian9@gmail.com

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-change-in-production
JWT_EXPIRES_IN=7d

# Email Templates
EMAIL_VERIFICATION_SUBJECT=Verifikasi Email - SIMANTAP KUBU RAYA
EMAIL_PASSWORD_RESET_SUBJECT=Reset Password - SIMANTAP KUBU RAYA
EMAIL_WELCOME_SUBJECT=Selamat Datang di SIMANTAP KUBU RAYA

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=759774329962582
CLOUDINARY_API_SECRET=sQMQF-TwcqKBplu4iyWwd7Zq8Nc
CLOUDINARY_FOLDER=SIMANTAP

# AI Chatbot Configuration (HuggingFace)
HUGGINGFACE_API_KEY=your_huggingface_api_key_here
HF_API_URL=https://router.huggingface.co/hf-inference/models
HF_MODEL=microsoft/DialoGPT-medium

# Alternative models if DialoGPT doesn't work well:
# HF_MODEL=bigscience/bloom-560m
# HF_MODEL=gpt2
# HF_MODEL=ai21labs/Jamba-Instruct-0.5